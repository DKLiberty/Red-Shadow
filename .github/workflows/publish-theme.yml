# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  publish-theme:
    runs-on: ubuntu-latest

    steps:
      # Шаг для клонирования кода вашего репозитория
      - name: Checkout repository
        uses: actions/checkout@v2

      # Установка Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'  # Здесь можно указать нужную вам версию Node.js

      # Установка модуля `probe-image-size`
      - name: Install dependencies
        run: |
          npm install probe-image-size

      # Выполнение скрипта с использованием `probe-image-size`
      - name: Run GitHub Script
        uses: actions/github-script@v6
        with:
          script: |
            const probe = require('probe-image-size');
            // Ваш скрипт для создания Pull Request или публикации темы
